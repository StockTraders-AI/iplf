# iPlatform Landing Page

Static reconstruction of the original landing page for iPlatform.

Changes:

- Brand text changed to `iPlatform`.
- Primary purple color changed to mint green and white.
- Original public image assets are kept in `images/`.

Run locally:

```powershell
python -m http.server 5177
```
